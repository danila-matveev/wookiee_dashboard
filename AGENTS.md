# Правила работы над проектом

## Коротко (основное)

- При старте задачи прочитать ARCHITECTURE.md, ADR.md, development-history.md; если файла нет - создать по шаблону ниже.
- Работать по существующим паттернам, правилам и соглашениям проекта.
- В конце работы обновить документацию, добавить запись в development-history.md и обновить .CLAUDE-context.md.

## Основные принципы

### 1. Всегда обновлять документацию и поддерживать консистентность проекта

1. **При получении задачи:**
   - Читать текущий ARCHITECTURE.md для понимания контекста. Этот документ нужен для описания актуальной архитектуры.
   - Проверять историю принятых решений ADR.md
   - Проверять историю выполненных работ development-history.md
   - Если ARCHITECTURE.md / ADR.md / development-history.md отсутствуют - создать по шаблонам ниже
   - Формулировать задачу на базе полученной информации. При наличии вопросов спросить пользователя

2. **Выполнить работу:**
   - Следовать существующим паттернам, правилам и соглашениям проекта

3. **Задокументировать изменения:**
   - Обновлять затронутую документацию и части проекта
   - При необходимости обновлять AGENTS.md, обеспечивая актуальность и минималистичность
   - Добавлять новые ADR, если принимаются архитектурные решения (см. раздел 3)
   - Записывать в development-history.md (см. раздел 4) запись о проделанной работе

### 2. ARCHITECTURE.md

ARCHITECTURE.md описывает актуальную архитектуру проекта и обновляется при значимых изменениях.

Если ARCHITECTURE.md отсутствует - создать файл по шаблону ниже.

Минимальный шаблон ARCHITECTURE.md:
```markdown
# ARCHITECTURE

## Обзор
## Контекст
## Ключевые компоненты
## Потоки данных
## Технологии и зависимости
## Нефункциональные требования и ограничения
## Roadmap
```

### 3. Документирование архитектурных решений (ADR.md)

**Когда использовать**: Для проектов с явной архитектурой, где важны технические решения.

Каждое важное техническое решение должно быть задокументировано как ADR. Примеры:

- Добавление критичной зависимости (например, брокер очередей или кэш)
- Переход между архитектурными стилями (например, REST -> GraphQL)
- Выбор стратегии хранения/доставки файлов (например, S3 vs локально)

**Где хранить**: В отдельном файле ADR.md

Если ADR.md отсутствует - создать файл и добавлять записи по шаблону ниже.

Шаблон записи ADR:

```markdown
# [YYYY-MM-DD HH:MM] ADR-XXX: Название решения
## Статус
Принято / Отклонено / Устарело
## Контекст
Описание проблемы или ситуации
## Решение
Что решили делать
## Обоснование
Почему именно так
## Последствия
Положительные и отрицательные
## Альтернативы рассмотрены
Что еще рассматривалось
```

### 4. Записи о проделанной работе (development-history.md)

Если development-history.md отсутствует - создать файл и вести записи по формату ниже.

#### Формат записи
```markdown
## [YYYY-MM-DD HH:MM] - Краткое описание итерации

### Что сделано
- Конкретная задача 1
- Конкретная задача 2
- Конкретная задача 3

### Зачем
Краткое объяснение цели итерации и принятых решений

### Обновлено
- [ ] ARCHITECTURE.md roadmap
- [ ] ADR (если есть)
- [ ] Тесты (если применимо)
- [ ] Документация

### Следующие шаги
- Что стоит сделать далее
```

### 5. Архивация и временные файлы

#### Архивация старых файлов

Чек-лист при замене важных файлов (README, конфиги, документация):
- Убедиться, что есть `archive/` (создать при отсутствии).
- Имя: `[YYYY-MM-DD_HH-MM]_original_name.ext`.
- В начало файла добавить префикс:

  ```markdown
  <!--
  АРХИВИРОВАНО: 2025-01-13 20:15
  ПРИЧИНА: Краткая причина
  НОВАЯ ВЕРСИЯ: ../path/to/new/file
  -->
  ```
- Переместить файл в `archive/`.
- Не архивировать: временные файлы, git-файлы, секреты, большие бинарники, файлы из `.gitignore`.

#### Временные файлы
- Все временные артефакты (выгрузки, кэши, временные или одноразовые скрипты, промежуточные JSON/CSV, локальные сохранения сообщений/медиа для отладки) складывать в директорию `temp/` в корне репозитория. Директорию создавать, если отсутствует.
- Не хранить временные файлы рядом с кодом или в корне; по окончании задачи очищать `temp/`.
- Добавлять новые временные файлы в `.gitignore` при необходимости.

### 6. Сохранение контекста между сессиями

#### В конце каждой сессии (.CLAUDE-context.md) 
- Если `.CLAUDE-context.md` отсутствует - создать файл по шаблону ниже
- Обновить `.CLAUDE-context.md` по шаблону:

  ```markdown
  # CLAUDE Context Snapshot
  Дата: [YYYY-MM-DD HH:MM]
  ## Текущая задача
  ## Статус: ✅ / 🔄 / ⬜
  ## Важные решения
  ## Следующий шаг
  ## Файлы в работе
  ```
- Зафиксировать завершено/в процессе/следующий шаг.
- Обновить список файлов в работе.

#### В начале новой сессии
- Прочитать `.CLAUDE-context.md` и свежие записи в `development-history.md`.
- Сообщить пользователю восстановленный контекст и план.

### 7. Definition of Done (Критерии завершенности)

#### Минимальный DoD для любой задачи:

- [ ] Функционал работает согласно требованиям
- [ ] Код следует стилю проекта
- [ ] Существующие тесты проходят
- [ ] Добавлены тесты для нового кода (или обоснование почему нет)
- [ ] Документация обновлена
- [ ] Запись в `development-history.md` добавлена

#### Дополнительно по типам задач:

**Новая фича:**
- [ ] Обновлен CHANGELOG (если ведется)
- [ ] Обновлена документация пользователя

**Багфикс:**
- [ ] Выявлена root cause
- [ ] Добавлен регрессионный тест

**Рефакторинг:**
- [ ] Функциональность не изменена (тесты подтверждают)
- [ ] ADR создан (для значимого рефакторинга)

**Документация:**
- [ ] Информация актуальна
- [ ] Примеры кода добавлены
- [ ] Ссылки работают

### 8. Видимость .env в IDE

Чтобы `.env` был виден пользователю в IDE, но скрыт от git и AI-агентов:

1. **`.vscode/settings.json`:**
   ```json
   {
     "explorer.excludeGitIgnore": false,
     "files.exclude": { "**/.env": false }
   }
   ```

2. **`.cursorignore`:**
   ```
   .env
   !.env.example
   ```

### 9. Логирование

- Добавляй логирование сетевых запросов и работы с внешними сервисами во всех скриптах, и логирование других действий, несущих потенциал ошибок. Цель: чтобы можно было отслеживать прогресс работы скрипта и отлаживание
- Используй минимально необходимый объём данных для логирования, если для отлаживания не потребовалось больше данных
- Логи клади в папку logs/
- Логи каждого скрипта клади в папку с названием скрипта и временем выполнения
- Логирование делай в формате .md и если нужно добавляй картинки (если используются в запросах или нужны для отлаживания)
- Одна запись = одно событие; указывай время и уровень (INFO/WARN/ERROR)
– Удаляй ставшие ненужными логи старше одного месяца, чтобы не забивать диск ненужными файлами, если нет иных инструкций.

Минимальный шаблон лога:
```markdown
# Log: <script-name>
Дата: YYYY-MM-DD HH:MM
Запуск: <command/args>

## События
- [HH:MM:SS] [INFO] start - ok
- [HH:MM:SS] [INFO] request <service> <method> <endpoint> - ok
- [HH:MM:SS] [ERROR] <action> - <error>
- [HH:MM:SS] [INFO] finish - ok
```
